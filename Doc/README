Run

    $ make extract

to extract code fragments from the Haskell files in ../src and
populate them into the Extracted/ directory.

Run

    $ make full

to run pdflatex twice, bibtex once, and pdflatex again to create the
PDF document.  The multiple runs resolve various cross-references in
the document etc.

Run

    $ make

to run pdflatex once, to create the PDF document.
